# Task B – Micro Scraper


1. `cd task-b`
2. `npm install`
3. `npm run dev`
4. Visit `http://localhost:3000` for the UI or hit `GET http://localhost:3000/api/scrape?url=https://example.com`

> The project uses the Playwright `postinstall` hook to download a Chromium binary automatically.
 